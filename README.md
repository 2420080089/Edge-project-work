# Fake News Detector (Edge - Python)

## Setup
pip install -r requirements.txt

## Train Model
python train.py

## Run App
python app.py

## Dataset
news.csv is already included (sample dataset).
You can replace it with a larger dataset for better accuracy.
